<div>
  <% @articles.each do |article| %>
    <div>
      <h3><%= article.title %></h3>
      <p><%= article.description %></p>
      <p><%= link_to 'Edit', edit_article_path(article) %></p>
    </div>
  <% end %>
</div>
